[build]
  command = "npm run build"
  publish = "."

[build.environment]
  NODE_VERSION = "22"
  DEMO_MODE = "true"
  # SESSION_SECRET must be set in Netlify Dashboard > Site settings > Environment variables
  # for security - never commit secrets to source code

[[plugins]]
  package = "@netlify/plugin-nextjs"
