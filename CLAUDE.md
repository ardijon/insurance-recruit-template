@AGENTS.md

<!--
This line is Claude Code's native import syntax — it loads AGENTS.md's full
content at session start, not just a hint the agent might follow. Add any
Claude-Code-only overrides below this line if you ever need them.
After running `graphify claude install`, Graphify appends its managed section
below as well.
-->
