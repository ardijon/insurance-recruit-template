# .graphifyignore — same syntax as .gitignore (merged with .gitignore; this file
# only ever excludes MORE, it never re-includes something .gitignore already ignores)

node_modules/
.next/
dist/
build/
out/
coverage/
*.generated.*
*.log

# Graphify's own output shouldn't feed back into itself
graphify-out/cache/
