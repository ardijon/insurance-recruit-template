# راهنمای حالت دمو

## حالت دمو چیست؟

حالت دمو به مشتریان اجازه می‌دهد سایت را قبل از خرید تست کنند.
در این حالت:

- **ورود به پنل مدیریت:** فقط دکمه "ورود" را بزنید (رمز لازم نیست)
- **تغییر اطلاعات:** تغییرات اعمال نمی‌شوند (فقط نمایش داده می‌شوند)
- **تغییر رمز عبور:** غیرفعال است
- **ارسال فرم درخواست:** فرم ثبت می‌شود اما در دیتابیس ذخیره نمی‌شود

## نحوه فعال‌سازی

### روش خودکار (توصیه شده)

فایل `demo.bat` را اجرا کنید. این اسکریپت:
1. فایل `.env.local` را با حالت دمو ایجاد می‌کند
2. دیتابیس را با داده‌های نمونه پر می‌کند
3. سرور Next.js را راه‌اندازی می‌کند
4. مرورگر را باز می‌کند

### روش دستی

1. فایل `.env.demo` را به `.env.local` کپی کنید:
   ```bash
   cp .env.demo .env.local
   ```

2. دیتابیس را ریست کنید:
   ```bash
   npm run seed
   ```

3. سرور را اجرا کنید:
   ```bash
   npm run dev
   ```

## لینک‌های مهم

| صفحه | آدرس |
|------|------|
| صفحه اصلی | http://localhost:3000 |
| فرم درخواست | http://localhost:3000/apply |
| پنل مدیریت | http://localhost:3000/admin |
| صفحه فرود | http://localhost:3000/landing.html |

## تفاوت حالت عادی و دمو

| ویژگی | حالت عادی | حالت دمو |
|-------|----------|---------|
| ورود به پنل | نیاز به رمز | فقط دکمه ورود |
| ذخیره تغییرات | بله | خیر |
| تغییر رمز | فعال | غیرفعال |
| ارسال فرم | ذخیره در DB | نمایش موفقیت |
| اعلان تلگرام | ارسال می‌شود | ارسال نمی‌شود |
