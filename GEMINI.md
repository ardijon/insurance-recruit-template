See AGENTS.md — all project rules live there. This file exists only so Gemini CLI
picks it up automatically. After running `graphify gemini install`, Graphify will also
append a managed section below.
