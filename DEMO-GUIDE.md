# راهنمای اجرای دمو — روز ارائه

## پیش‌نیازها
- VPN روشن باشه (برای دسترسی به Cloudflare)
- لپ‌تاپ به اینترنت وصل باشه
- پروژه در مسیر `D:\New Projects\Insurance\manager` باشه

## اجرا (۲ دقیقه قبل از شروع ارائه)

### قدم ۱: VPN روشن کن
فیلترشکن رو روشن کن و مطمئن شو وصله.

### قدم ۲: فایل دیتابیس قبلی رو پاک کن (اختیاری)
اگه می‌خوای داده‌های نمونه جدید لود بشه:
```
del "D:\New Projects\Insurance\manager\data\app.db"
```

### قدم ۳: اسکریپت دمو رو اجرا کن
روی فایل `start-demo.bat` دابل‌کلیک کن.
(یا راست کلیک روی `start-demo.ps1` → Run with PowerShell)

دو تا پنجره باز می‌شه:
1. پنجره Next.js (پشت زمینه)
2. پنجره Cloudflare Tunnel (جلو) → **لینک عمومی اینجا نمایش داده می‌شه**

### قدم ۴: لینک رو روی گوشی باز کن
لینکی شبیه این میاد:
```
https://xxx-xxx-xxx.trycloudflare.com
```
این لینک رو روی گوشی باز کن.

### قدم ۵: ارائه رو شروع کن
- صفحه اصلی: پروفایل مدیر، دیوار موفقیت، مسیر رشد، سوالات متداول
- فرم ثبت‌نام: `/apply`
- پنل مدیریت: `/admin` (رمز: admin123)

## نکات مهم

### سرعت
- فونت وزیرمتن محلی شده (از Google لود نمی‌شه)
- عکس‌های پروفایل已有 هستن، سنگین نیستن
- اگه کند بود، عکس ۱.۵ مگابایتی رو حذف کن:
  ```
  del "D:\New Projects\Insurance\manager\public\uploads\profile\manager_1785077863714.png"
  ```

### اگه ارور داد
- `npm run dev` اجرا نمی‌شه → اول `npm install` رو اجرا کن
- cloudflared کار نمی‌کنه → VPN رو قطع و وصل کن
- پورت 3000 اشغاله → `netstat -aon | findstr :3000` و بعد `taskkill /PID <شماره> /F`

### بعد از ارائه
پنجره‌ها رو ببند یا `Ctrl+C` بزن.

## اطلاعات ورود به پنل مدیریت
- آدرس: `/admin`
- رمز: `admin123`
